import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.bpurihk.planetevolution',
  appName: 'Planet Evolution',
  webDir: 'dist/public',
};

export default config;